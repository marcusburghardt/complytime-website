---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# website Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-06

## Active Technologies
- Hugo extended ≥ 0.155.1 (SSG), Node.js ≥ 22 (npm pipeline), Go 1.25+ (sync tool — unchanged by this feature) + `@thulite/doks-core` (provides Bootstrap 5 and `data-bs-theme` switching); no new dependencies added (013-sync-summary-arch-diagram)
- Static PNG files committed to `static/images/`; Hugo shortcode in `layouts/shortcodes/`; SCSS rules in `assets/scss/common/_custom.scss` (013-sync-summary-arch-diagram)

- Hugo 0.155.1 extended (site framework); SCSS (compiled by Hugo pipes via node-sass/dart-sass) + Thulite Doks theme (Bootstrap 5.x — provides `data-bs-theme` attribute for dark/light switching); PostCSS + PurgeCSS (production CSS pipeline) (013-sync-summary-arch-diagram)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Hugo 0.155.1 extended (site framework); SCSS (compiled by Hugo pipes via node-sass/dart-sass)

## Code Style

Hugo 0.155.1 extended (site framework); SCSS (compiled by Hugo pipes via node-sass/dart-sass): Follow standard conventions

## Recent Changes
- 013-sync-summary-arch-diagram: Added Hugo extended ≥ 0.155.1 (SSG), Node.js ≥ 22 (npm pipeline), Go 1.25+ (sync tool — unchanged by this feature) + `@thulite/doks-core` (provides Bootstrap 5 and `data-bs-theme` switching); no new dependencies added

- 013-sync-summary-arch-diagram: Added Hugo 0.155.1 extended (site framework); SCSS (compiled by Hugo pipes via node-sass/dart-sass) + Thulite Doks theme (Bootstrap 5.x — provides `data-bs-theme` attribute for dark/light switching); PostCSS + PurgeCSS (production CSS pipeline)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
